apiVersion: v1
kind: Service
metadata:
  name: end-end-devops-service
spec:
  type: LoadBalancer
  selector:
    app: end-end-devops-project
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
